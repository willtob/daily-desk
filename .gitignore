# Python
__pycache__/
*.py[cod]
.venv/

# Secrets
.env

# Embedding cache (Phase 3) — regenerates itself, just costs an API call
.cache/

# Generated digests (Phase 5), the API payload (Phase 6), and the seen store
digests/*.md
digests/*.json
# Cached TTS audio (Phase 8) — regenerates on demand, ~700 KB per article
digests/audio/

# macOS
.DS_Store
