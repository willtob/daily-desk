/*
 * wifi_credentials.example.h — template.
 *
 * Copy to wifi_credentials.h and fill in your network. That file is
 * gitignored so the real credentials never reach the repo.
 *
 *   cp src/wifi_credentials.example.h src/wifi_credentials.h
 */
#pragma once

#define WIFI_SSID        "your-network-name"
#define WIFI_PASSWORD    "your-network-password"
